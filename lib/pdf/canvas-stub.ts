// Stub for the optional native `canvas` package that pdfjs-dist probes for when
// it believes it is running under Node. NusaPDF renders exclusively in the
// browser, so this module exists only to satisfy the resolver.
export default {};
